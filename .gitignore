# Build output
build/

# Sony Camera Remote SDK — proprietary, not ours to redistribute.
# Populate these from your own copy of the SDK package: make sdk
lib/
include/CRSDK/

# Python
__pycache__/
*.pyc

# Runtime state (rigd writes these outside the repo, but be explicit)
*.jsonl
.DS_Store
